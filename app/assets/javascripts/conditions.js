function conditions(){
  if (window.location.pathname == "/" ){
    document.getElementsByTagName("nav").style.display = "none";
    document.getElementsByTagName("body").style.backgroundImage = "none";

  }
// this value is returned when the function is called in calculatePricing.
}