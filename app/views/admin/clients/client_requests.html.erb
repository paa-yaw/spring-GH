<h1><%= @client.fullname %>'s requests</h1>

<% if @client.requests.empty? %>
<h3>No requests at the moment</h3>

<% else %>

<div class="row">
	<div class="col-lg-12 col-md-12">  
		<div class="panel panel-default">
			<div class="panel-heading">
				<h2><i class="fa fa-user"></i><strong><%= @client.fullname %>'s requests</strong></h2>
			</div>
			<div class="panel-body">
				<table class="table bootstrap-datatable countries">
					<thead>
						<tr>
							<th></th>
							<th>Request ID</th>
							<th>created at</th>
							<th>Status</th>
						</tr>
					</thead>  

					<% @client.requests.each do |client_request| %>
					<% if client_request.resolved == true %>

					<tbody>
						<tr>
							<td></td>
							<td><%= client_request.id %></td>
							<td><%= client_request.created_at %></td>
							<td><%= link_to "resolved", admin_request_path(client_request), class: "btn btn-success" %></td>
						</tr>
						<% else %> 
						<tr>
							<td></td>
							<td><%= client_request.id %></td>
							<td><%= client_request.created_at %></td>
							<td><%= link_to "unresolved", admin_request_path(client_request), class: "btn btn-danger" %></td>
						</tr>
					</tbody>
					<% end %>  
					<% end %>
				</table>
			</div>
		</div>
	</div>
</div>	

<% end %>




<div class="row">
<div class="container">
<%= link_to fa_icon("plus") + "Create Request", new_admin_client_request_path(@client), class: "btn btn-primary" %> 
<%= link_to "Back", :back, class: "btn btn-primary" %>
</div>
</div>