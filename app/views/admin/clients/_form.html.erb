<%= simple_form_for([:admin, client]) do |f| %>
  <%= f.input :fullname %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :phone_number %>
  <%= f.input :location %>

  <%= f.button :submit, "Create User", class: "btn-primary" %>
<% end %>