<!DOCTYPE html>
<html>
<head>
  <title>Spring</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="google-site-verification" content="ekk5nq_UoqHnmI-X2AO7JNLZsgclWEvyO1Y5OTweFQ4" />
</head>

<body>
      <nav id="navigation" class="navbar navbar-default navbar-fixed-top" role="navigation" style="box-shadow: 0px 1px 1px #b3b3b3;">
              <div class="container-fluid">
              <!-- Brand and toggle  get grouped for better mobile display -->
                  <div class="navbar-header">
                      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-rj-collapse">
                          <span class="sr-only">Toggle navigation</span>
                          <span class="icon-bar"></span>
                          <span class="icon-bar"></span>
                          <span class="icon-bar"></span>
                      </button>
                      <a href="/" class="spring-brand">
                        <%= image_tag("logo.png", :alt => "spring logo", :class => "img-responsive") %>
                      </a>
                  </div>

            <!-- //.navbar-header -->
             <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="navbar-collapse collapse navbar-rj-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <li class="page-scroll">
                        <% if current_client && current_client.admin == false %>
                          <%= link_to "Requests", my_requests_path %>
                        <% end %>
                    </li>

                    <% if current_client %>

                    <li class="page-scroll">
                         <%= link_to "About" %>
                    </li> 

                     <li class="page-scroll">
                        <%= link_to current_client.fullname, edit_client_registration_path %>
                    </li>

                    <li class="page-scroll">          
                        <%= link_to "Log Out", destroy_client_session_path, method: :delete,
                              data: {confirm: "#{current_client.email}, are you sure you want to log out?"} %>
                    </li>      

                    <% else %>
                        <li class="page-scroll">  
                          <%= link_to "Log In", new_client_session_path %>
                        </li> 
                    <% end %>
                </ul>
            </div>
            <!-- //.navbar-collapse -->
        </div>
        <!-- //.container -->
    </nav>

 <% flash.each do |key, message| %>
  <p><%= message %></p>
 <% end %>
 <style>
 #region{
  padding-top: 50px;
 }
 </style>
 <div id="region">
   <%= yield %>
 </div>
</body>
</html>
